kind = "web"
previewPath = "/"
title = "OrchestrateAI"
version = "1.0.0"
id = "artifacts/orchestrate-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18804

[services.development]
run = "pnpm --filter @workspace/orchestrate-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/orchestrate-ai", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/orchestrate-ai", "run", "serve" ]

[services.env]
PORT = "18804"
BASE_PATH = "/"
